# bullinger-line-alignment-mwe

OCR/HTR generation for Method 2 is described in docs/ocr_pipeline.md. Run `python scripts/make_ocr_outputs.py --dataset bullinger_handwritten` to populate ocr/<id>.txt for a dataset.